type Mutation {
    createUser(input: CreateUserInput): User
    signInUser(input: CreateUserInput): SignInPayload
}

type Query {
    findWordsBy(input: findWordsInput): [Word]
    findUsers: [User]
}

schema {
    query: Query
    mutation: Mutation
}